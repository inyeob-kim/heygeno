# 🐾 HeyZeno 온보딩 플로우 설계 문서

**iOS 감성의 반려동물 프로필 설정 온보딩 UX/UI 설계**

---

## 📋 목차

1. [화면별 와이어 구조 및 전환 규칙](#1-화면별-와이어-구조-및-전환-규칙)
2. [한국어 마이크로카피](#2-한국어-마이크로카피)
3. [데이터 스키마 및 Validation 규칙](#3-데이터-스키마-및-validation-규칙)
4. [Flutter 파일 구조 제안](#4-flutter-파일-구조-제안)
5. ["귀찮지 않게" 만드는 디테일 10가지](#5-귀찮지-않게-만드는-디테일-10가지)

---

## 1. 화면별 와이어 구조 및 전환 규칙

### 공통 레이아웃 구조

```
OnboardingScreen
├── SafeArea
│   ├── Column
│   │   ├── OnboardingHeader (상단 고정)
│   │   │   ├── ProgressBar (진행률)
│   │   │   └── BackButton (첫 화면 제외)
│   │   │
│   │   ├── Expanded
│   │   │   └── SingleChildScrollView
│   │   │       └── Padding
│   │   │           └── Column (화면별 컨텐츠)
│   │   │               ├── EmojiIcon (큰 이모지/아이콘)
│   │   │               ├── TitleText
│   │   │               ├── SubtitleText
│   │   │               └── ContentWidget (입력/선택 UI)
│   │   │
│   │   └── OnboardingFooter (하단 고정)
│   │       └── AppPrimaryButton (CTA)
│   │
│   └── KeyboardAvoidingView (입력 화면만)
```

### 화면별 상세 구조

#### 1️⃣ Welcome + 닉네임

```
WelcomeNicknameScreen
├── OnboardingHeader (progress: 1/11)
├── Content
│   ├── EmojiIcon: 😊 (size: 80)
│   ├── TitleText: "안녕하세요 😊"
│   ├── SubtitleText: "헤이제노에서 쓸 닉네임만 먼저 정해볼까요?"
│   └── InputSection
│       ├── TextField (닉네임 입력)
│       │   ├── hintText: "닉네임을 입력해주세요"
│       │   ├── maxLength: 12
│       │   └── counterText: "${length}/12"
│       └── RandomNicknameButton
│           ├── Icon: 🎲
│           └── Text: "추천받기"
└── Footer
    └── NextButton (disabled: nickname.length < 2)
```

**전환 규칙:**
- 진입: 앱 첫 실행 시 또는 onboarding_completed = false
- 다음: nickname.length >= 2 → Step 2
- 뒤로: 첫 화면이므로 비활성화

---

#### 2️⃣ 아이 이름

```
PetNameScreen
├── OnboardingHeader (progress: 2/11, backButton: true)
├── Content
│   ├── EmojiIcon: 🐾 (size: 80)
│   ├── TitleText: "우리 아이 이름은요? 🐾"
│   └── InputSection
│       └── TextField
│           ├── hintText: "이름을 입력해주세요"
│           ├── maxLength: 20
│           └── textCapitalization: TextCapitalization.words
└── Footer
    └── NextButton (disabled: name.isEmpty)
```

**전환 규칙:**
- 진입: Step 1 완료
- 다음: name.isNotEmpty → Step 3
- 뒤로: Step 1로 이동

---

#### 3️⃣ 종 선택 (강아지/고양이)

```
SpeciesSelectionScreen
├── OnboardingHeader (progress: 3/11, backButton: true)
├── Content
│   ├── EmojiIcon: 🐶🐱 (size: 80)
│   ├── TitleText: "어떤 친구인가요? 🐶🐱"
│   └── SelectionCards
│       ├── SpeciesCard (dog)
│       │   ├── Icon: 🐶 (size: 60)
│       │   ├── Text: "강아지"
│       │   └── selected: species == 'dog'
│       └── SpeciesCard (cat)
│           ├── Icon: 🐱 (size: 60)
│           ├── Text: "고양이"
│           └── selected: species == 'cat'
└── Footer
    └── NextButton (disabled: species == null)
```

**전환 규칙:**
- 진입: Step 2 완료
- 다음: species != null → Step 4
- 뒤로: Step 2로 이동

---

#### 4️⃣ 생년월일/대략 나이 모드

```
BirthModeSelectionScreen
├── OnboardingHeader (progress: 4/11, backButton: true)
├── Content
│   ├── EmojiIcon: 🎂 (size: 80)
│   ├── TitleText: "나이를 어떻게 알려주실래요? 🎂"
│   └── ModeSelection
│       ├── BirthModeCard (exact)
│       │   ├── Icon: 📅
│       │   ├── Text: "생년월일 알아요"
│       │   └── selected: birthMode == 'exactBirthdate'
│       └── BirthModeCard (approx)
│           ├── Icon: 🎈
│           ├── Text: "대략적인 나이만 알아요"
│           └── selected: birthMode == 'approxAge'
│
│   └── ConditionalContent
│       ├── if (birthMode == 'exactBirthdate')
│       │   └── DatePicker
│       │       ├── iOS CupertinoDatePicker
│       │       └── initialDate: DateTime.now().subtract(1.year)
│       └── if (birthMode == 'approxAge')
│           └── AgeStepper
│               ├── YearsStepper (0~20)
│               └── MonthsStepper (0~11)
└── Footer
    └── NextButton (disabled: birthMode == null || (exact && birthdate == null) || (approx && ageYears == null))
```

**전환 규칙:**
- 진입: Step 3 완료
- 다음: birthMode + 해당 데이터 입력 완료 → Step 5 (dog) or Step 6 (cat)
- 뒤로: Step 3로 이동

---

#### 5️⃣ 품종 선택 (강아지일 때)

```
BreedSelectionScreen
├── OnboardingHeader (progress: 5/11, backButton: true)
├── Content
│   ├── EmojiIcon: 🐶 (size: 80)
│   ├── TitleText: "어떤 품종인가요? 🐶"
│   └── BreedSearchSection
│       ├── SearchBar
│       │   ├── hintText: "품종 검색 (예: 골든리트리버)"
│       │   └── onChanged: filter breeds
│       └── BreedList
│           ├── PopularBreedsSection (상단 고정)
│           │   └── BreedChip[] (인기 품종)
│           └── AllBreedsSection
│               └── BreedChip[] (검색 결과)
│           └── SpecialOption
│               └── BreedChip ("믹스/잘 모르겠어요")
└── Footer
    └── NextButton (disabled: breed == null)
```

**전환 규칙:**
- 진입: Step 4 완료 && species == 'dog'
- 다음: breed != null → Step 6
- 뒤로: Step 4로 이동
- 스킵: 없음 (필수)

**고양이일 때:**
- species == 'cat'이면 Step 4 → Step 6 (이 화면 스킵)

---

#### 6️⃣ 성별 + 중성화

```
SexAndNeuteredScreen
├── OnboardingHeader (progress: 6/11, backButton: true)
├── Content
│   ├── EmojiIcon: ✨ (size: 80)
│   ├── TitleText: "성별과 중성화 정보를 알려주세요 ✨"
│   └── SelectionSections
│       ├── SexSection
│       │   ├── Label: "성별"
│       │   └── SexCards
│       │       ├── SexCard (male)
│       │       │   ├── Icon: ♂️
│       │       │   ├── Text: "남아"
│       │       │   └── selected: sex == 'male'
│       │       └── SexCard (female)
│       │           ├── Icon: ♀️
│       │           ├── Text: "여아"
│       │           └── selected: sex == 'female'
│       └── NeuteredSection
│           ├── Label: "중성화"
│           └── NeuteredCards
│               ├── NeuteredCard (yes)
│               │   ├── Text: "했어요"
│               │   └── selected: neutered == 'yes'
│               ├── NeuteredCard (no)
│               │   ├── Text: "안 했어요"
│               │   └── selected: neutered == 'no'
│               └── NeuteredCard (unknown)
│                   ├── Text: "잘 모르겠어요"
│                   └── selected: neutered == 'unknown'
└── Footer
    └── NextButton (disabled: sex == null || neutered == null)
```

**전환 규칙:**
- 진입: Step 5 완료 (dog) or Step 4 완료 (cat)
- 다음: sex != null && neutered != null → Step 7
- 뒤로: 이전 단계로 이동

---

#### 7️⃣ 몸무게

```
WeightScreen
├── OnboardingHeader (progress: 7/11, backButton: true)
├── Content
│   ├── EmojiIcon: ⚖️ (size: 80)
│   ├── TitleText: "몸무게는 얼마인가요? ⚖️"
│   ├── SubtitleText: "최근 측정 기준이면 좋아요"
│   └── WeightInputSection
│       ├── WeightDisplay
│       │   ├── LargeNumber: "${weightKg}kg"
│       │   └── Animation: scale on change
│       └── WeightControls
│           ├── QuickAdjustButtons
│           │   ├── Button (-0.1kg)
│           │   └── Button (+0.1kg)
│           └── NumberPad
│               └── CustomNumberPad (0~99.9)
└── Footer
    └── NextButton (disabled: weightKg == null || weightKg <= 0)
```

**전환 규칙:**
- 진입: Step 6 완료
- 다음: weightKg > 0 → Step 8
- 뒤로: Step 6로 이동

---

#### 8️⃣ 비만도 (Body Condition Score)

```
BodyConditionScoreScreen
├── OnboardingHeader (progress: 8/11, backButton: true)
├── Content
│   ├── EmojiIcon: 🧡 (size: 80)
│   ├── TitleText: "체형은 어느 쪽에 가까울까요? 🧡"
│   └── BCSSection
│       ├── CharacterAnimation
│       │   └── AnimatedCharacter (1~9에 따라 실루엣 변화)
│       │       └── HeartPopAnimation (첫 선택 시 1회)
│       ├── Slider
│       │   ├── min: 1
│       │   ├── max: 9
│       │   ├── value: bcs
│       │   └── onChanged: update BCS + animate character
│       ├── Labels
│       │   ├── LeftLabel: "마른 편"
│       │   └── RightLabel: "통통한 편"
│       └── FeedbackText
│           └── Text (구간별 설명)
│               ├── 1~3: "조금 마른 편이에요"
│               ├── 4~6: "딱 좋아요! 💚"
│               └── 7~9: "조금 관리해볼까요?"
└── Footer
    └── NextButton (disabled: bcs == null)
```

**전환 규칙:**
- 진입: Step 7 완료
- 다음: bcs != null → Step 9
- 뒤로: Step 7로 이동

---

#### 9️⃣ 건강 고민 선택

```
HealthConcernsScreen
├── OnboardingHeader (progress: 9/11, backButton: true)
├── Content
│   ├── EmojiIcon: 🩺 (size: 80)
│   ├── TitleText: "요즘 신경 쓰이는 건강 고민이 있나요? 🩺"
│   └── ChipSelection
│       └── HealthConcernChips
│           ├── Chip ("없어요", exclusive: true)
│           ├── Chip ("알레르기")
│           ├── Chip ("장/소화")
│           ├── Chip ("치아/구강")
│           ├── Chip ("비만")
│           ├── Chip ("호흡기")
│           ├── Chip ("피부/털")
│           ├── Chip ("관절")
│           ├── Chip ("눈/눈물")
│           ├── Chip ("신장/요로")
│           ├── Chip ("심장")
│           └── Chip ("노령")
└── Footer
    └── NextButton (disabled: healthConcerns.isEmpty)
```

**전환 규칙:**
- 진입: Step 8 완료
- 다음: healthConcerns.isNotEmpty → Step 10
- 뒤로: Step 8로 이동
- 로직: "없어요" 선택 시 나머지 자동 해제

---

#### 🔟 음식 알레르기

```
FoodAllergiesScreen
├── OnboardingHeader (progress: 10/11, backButton: true)
├── Content
│   ├── EmojiIcon: 🍗 (size: 80)
│   ├── TitleText: "피해야 하는 재료가 있나요? 🍗"
│   └── ChipSelection
│       └── AllergyChips
│           ├── Chip ("없어요", exclusive: true)
│           ├── Chip ("소고기")
│           ├── Chip ("닭고기")
│           ├── Chip ("돼지고기")
│           ├── Chip ("오리고기")
│           ├── Chip ("양고기")
│           ├── Chip ("생선")
│           ├── Chip ("계란")
│           ├── Chip ("유제품")
│           ├── Chip ("밀/글루텐")
│           ├── Chip ("옥수수")
│           ├── Chip ("콩")
│           └── Chip ("기타", withTextField: true)
└── Footer
    └── NextButton (disabled: foodAllergies.isEmpty)
```

**전환 규칙:**
- 진입: Step 9 완료
- 다음: foodAllergies.isNotEmpty → Step 11
- 뒤로: Step 9로 이동
- 로직: "없어요" 선택 시 나머지 자동 해제

---

#### 1️⃣1️⃣ 사진 업로드 (선택)

```
PhotoUploadScreen
├── OnboardingHeader (progress: 11/11, backButton: true)
├── Content
│   ├── EmojiIcon: 📸 (size: 80)
│   ├── TitleText: "아이 사진을 올려볼까요? 📸"
│   ├── SubtitleText: "나중에 해도 괜찮아요"
│   └── PhotoSection
│       ├── if (photoUrl == null)
│       │   └── PhotoPlaceholder
│       │       ├── Icon: 📷
│       │       └── Text: "사진을 선택해주세요"
│       └── if (photoUrl != null)
│           └── PhotoPreview
│               ├── Image (circular, 200x200)
│               └── ChangePhotoButton
│       └── ActionButtons
│           ├── SelectPhotoButton ("사진 선택")
│           ├── TakePhotoButton ("사진 찍기")
│           └── SkipButton ("건너뛰기")
└── Footer
    └── StartButton ("헤이제노 시작하기")
```

**전환 규칙:**
- 진입: Step 10 완료
- 다음: "헤이제노 시작하기" 클릭 → 메인 홈으로 replace navigation
- 뒤로: Step 10로 이동
- 스킵: 사진 없이도 진행 가능

---

### 전체 플로우 다이어그램

```
[앱 시작]
    ↓
[device_uid 체크]
    ↓
[onboarding_completed 체크]
    ↓
┌─────────────────┐
│  Step 1: 닉네임 │
└────────┬────────┘
         ↓
┌─────────────────┐
│  Step 2: 이름   │
└────────┬────────┘
         ↓
┌─────────────────┐
│  Step 3: 종     │
└────────┬────────┘
         ↓
┌─────────────────┐
│  Step 4: 나이   │
└────────┬────────┘
         ↓
    ┌────┴────┐
    │         │
[강아지]   [고양이]
    │         │
    ↓         ↓
┌──────┐   ┌──────┐
│Step 5│   │Step 6│
│품종  │   │성별  │
└──┬───┘   └──┬───┘
   │          │
   └────┬─────┘
        ↓
┌─────────────────┐
│  Step 6/7: 성별 │
└────────┬────────┘
         ↓
┌─────────────────┐
│  Step 7/8: 몸무게│
└────────┬────────┘
         ↓
┌─────────────────┐
│  Step 8/9: BCS  │
└────────┬────────┘
         ↓
┌─────────────────┐
│  Step 9/10: 건강│
└────────┬────────┘
         ↓
┌─────────────────┐
│ Step 10/11: 알레│
└────────┬────────┘
         ↓
┌─────────────────┐
│ Step 11/12: 사진│
└────────┬────────┘
         ↓
[시작하기 버튼]
    ↓
[데이터 저장]
    ↓
[메인 홈]
```

---

## 2. 한국어 마이크로카피

### 공통 카피

#### 진행률 표시
- **형식**: "1 / 11" 또는 "1단계"
- **위치**: 상단 헤더

#### 버튼
- **다음**: "다음"
- **시작하기**: "헤이제노 시작하기"
- **건너뛰기**: "건너뛰기"
- **뒤로가기**: 아이콘만 (텍스트 없음)

#### 에러 메시지
- **필수 입력 누락**: "이 정보는 꼭 필요해요"
- **형식 오류**: "올바른 형식으로 입력해주세요"
- **범위 초과**: "범위를 벗어났어요"

---

### 화면별 카피

#### 1️⃣ Welcome + 닉네임

**타이틀**: "안녕하세요 😊"

**서브멘트**: "헤이제노에서 쓸 닉네임만 먼저 정해볼까요?"

**입력 힌트**: "닉네임을 입력해주세요"

**추천 버튼**: "🎲 추천받기"

**에러 메시지**:
- 2자 미만: "닉네임은 2자 이상이어야 해요"
- 12자 초과: "닉네임은 12자 이하여야 해요"

**CTA**: "다음"

---

#### 2️⃣ 아이 이름

**타이틀**: "우리 아이 이름은요? 🐾"

**서브멘트**: 없음

**입력 힌트**: "이름을 입력해주세요"

**에러 메시지**:
- 빈 값: "이름을 입력해주세요"

**CTA**: "다음"

---

#### 3️⃣ 종 선택

**타이틀**: "어떤 친구인가요? 🐶🐱"

**서브멘트**: 없음

**선택지**:
- "강아지"
- "고양이"

**에러 메시지**: 없음 (선택 필수)

**CTA**: "다음"

---

#### 4️⃣ 생년월일/나이 모드

**타이틀**: "나이를 어떻게 알려주실래요? 🎂"

**서브멘트**: 없음

**선택지**:
- "생년월일 알아요"
- "대략적인 나이만 알아요"

**DatePicker 힌트**: "생년월일을 선택해주세요"

**나이 입력 힌트**: 
- "살" (연령)
- "개월" (개월 수)

**에러 메시지**:
- 모드 미선택: "나이 정보를 알려주세요"
- 생년월일 미선택: "생년월일을 선택해주세요"
- 나이 미입력: "나이를 입력해주세요"

**CTA**: "다음"

---

#### 5️⃣ 품종 선택 (강아지)

**타이틀**: "어떤 품종인가요? 🐶"

**서브멘트**: 없음

**검색 힌트**: "품종 검색 (예: 골든리트리버)"

**인기 품종 섹션**: "인기 품종"

**특수 옵션**: "믹스/잘 모르겠어요"

**에러 메시지**: 없음 (선택 필수)

**CTA**: "다음"

---

#### 6️⃣ 성별 + 중성화

**타이틀**: "성별과 중성화 정보를 알려주세요 ✨"

**서브멘트**: 없음

**성별 섹션**:
- 라벨: "성별"
- 선택지: "남아", "여아"

**중성화 섹션**:
- 라벨: "중성화"
- 선택지: "했어요", "안 했어요", "잘 모르겠어요"

**에러 메시지**: 없음 (선택 필수)

**CTA**: "다음"

---

#### 7️⃣ 몸무게

**타이틀**: "몸무게는 얼마인가요? ⚖️"

**서브멘트**: "최근 측정 기준이면 좋아요"

**입력 힌트**: "0.0"

**빠른 조절 버튼**: "-0.1kg", "+0.1kg"

**에러 메시지**:
- 0 이하: "몸무게를 입력해주세요"
- 범위 초과: "0.1kg ~ 99.9kg 사이로 입력해주세요"

**CTA**: "다음"

---

#### 8️⃣ 비만도 (BCS)

**타이틀**: "체형은 어느 쪽에 가까울까요? 🧡"

**서브멘트**: 없음

**슬라이더 라벨**:
- 왼쪽: "마른 편"
- 오른쪽: "통통한 편"

**구간별 피드백**:
- 1~3: "조금 마른 편이에요"
- 4~6: "딱 좋아요! 💚"
- 7~9: "조금 관리해볼까요?"

**에러 메시지**: 없음 (선택 필수)

**CTA**: "다음"

---

#### 9️⃣ 건강 고민

**타이틀**: "요즘 신경 쓰이는 건강 고민이 있나요? 🩺"

**서브멘트**: 없음

**선택지**:
- "없어요" (exclusive)
- "알레르기"
- "장/소화"
- "치아/구강"
- "비만"
- "호흡기"
- "피부/털"
- "관절"
- "눈/눈물"
- "신장/요로"
- "심장"
- "노령"

**에러 메시지**: 없음 (최소 1개 선택, "없어요" 포함)

**CTA**: "다음"

---

#### 🔟 음식 알레르기

**타이틀**: "피해야 하는 재료가 있나요? 🍗"

**서브멘트**: 없음

**선택지**:
- "없어요" (exclusive)
- "소고기"
- "닭고기"
- "돼지고기"
- "오리고기"
- "양고기"
- "생선"
- "계란"
- "유제품"
- "밀/글루텐"
- "옥수수"
- "콩"
- "기타" (텍스트 입력 필드 포함)

**에러 메시지**: 없음 (최소 1개 선택, "없어요" 포함)

**CTA**: "다음"

---

#### 1️⃣1️⃣ 사진 업로드

**타이틀**: "아이 사진을 올려볼까요? 📸"

**서브멘트**: "나중에 해도 괜찮아요"

**플레이스홀더**: "사진을 선택해주세요"

**액션 버튼**:
- "사진 선택"
- "사진 찍기"
- "건너뛰기"

**에러 메시지**: 없음 (선택 사항)

**CTA**: "헤이제노 시작하기"

---

## 3. 데이터 스키마 및 Validation 규칙

### 로컬 저장소 (Secure Storage)

```dart
// 키 목록
'device_uid'              // String (UUID v4)
'onboarding_completed'    // bool
'onboarding_step'         // int (1~11, 마지막 완료 단계)
'draft_nickname'          // String?
'draft_pet_profile'       // String? (JSON)
```

### User 모델

```dart
class User {
  final String deviceUid;      // 필수, UUID v4
  final String nickname;        // 필수, 2~12자
  final DateTime createdAt;     // 자동 생성
}
```

**Validation 규칙:**
- `deviceUid`: UUID v4 형식, null 불가
- `nickname`: 2~12자, 한글/영문/숫자/언더스코어 허용, 공백 불가

---

### PetProfile 모델

```dart
class PetProfile {
  // 필수 필드
  final String name;                    // 1~20자
  final String species;                  // 'dog' | 'cat'
  final String birthMode;               // 'exactBirthdate' | 'approxAge'
  final String sex;                      // 'male' | 'female'
  final String neutered;                 // 'yes' | 'no' | 'unknown'
  final double weightKg;                 // 0.1 ~ 99.9
  final int bodyConditionScore;         // 1 ~ 9
  final Set<String> healthConcerns;     // 최소 1개 (빈 집합 불가)
  final Set<String> foodAllergies;       // 최소 1개 (빈 집합 불가)
  
  // 조건부 필수
  final DateTime? birthdate;             // birthMode == 'exactBirthdate'일 때 필수
  final int? ageYears;                   // birthMode == 'approxAge'일 때 필수
  final int? ageMonths;                  // birthMode == 'approxAge'일 때 선택 (0~11)
  final String? breed;                   // species == 'dog'일 때 필수, 'cat'일 때 선택
  
  // 선택 필드
  final String? photoUrl;                // 선택, URL 또는 base64
}
```

---

### Validation 규칙 상세

| 필드 | 타입 | 필수 여부 | 규칙 | 에러 메시지 |
|------|------|----------|------|------------|
| `name` | String | ✅ | 1~20자, 공백 제거 후 검증 | "이름을 입력해주세요" |
| `species` | String | ✅ | 'dog' 또는 'cat' | "종을 선택해주세요" |
| `birthMode` | String | ✅ | 'exactBirthdate' 또는 'approxAge' | "나이 정보 방식을 선택해주세요" |
| `birthdate` | DateTime? | 조건부 | birthMode == 'exactBirthdate'일 때 필수, 미래 날짜 불가 | "생년월일을 선택해주세요" |
| `ageYears` | int? | 조건부 | birthMode == 'approxAge'일 때 필수, 0~20 | "나이를 입력해주세요" |
| `ageMonths` | int? | 조건부 | birthMode == 'approxAge'일 때 선택, 0~11 | - |
| `breed` | String? | 조건부 | species == 'dog'일 때 필수 | "품종을 선택해주세요" |
| `sex` | String | ✅ | 'male' 또는 'female' | "성별을 선택해주세요" |
| `neutered` | String | ✅ | 'yes', 'no', 'unknown' 중 하나 | "중성화 정보를 선택해주세요" |
| `weightKg` | double | ✅ | 0.1 ~ 99.9, 소수점 1자리 | "몸무게를 입력해주세요" |
| `bodyConditionScore` | int | ✅ | 1 ~ 9 | "체형을 선택해주세요" |
| `healthConcerns` | Set<String> | ✅ | 최소 1개, "없어요" 포함 가능 | "건강 고민을 선택해주세요" |
| `foodAllergies` | Set<String> | ✅ | 최소 1개, "없어요" 포함 가능 | "알레르기 정보를 선택해주세요" |
| `photoUrl` | String? | ❌ | URL 형식 또는 base64 | - |

---

### 완료 조건

**온보딩 완료 조건:**
1. `deviceUid` 존재
2. `nickname` 유효 (2~12자)
3. `petProfile`의 모든 필수 필드 유효
4. "헤이제노 시작하기" 버튼 클릭

**재실행 시 체크:**
```dart
if (deviceUid != null && onboardingCompleted == true) {
  // 메인 홈으로 이동
} else if (deviceUid != null && onboardingStep > 0) {
  // 마지막 단계로 resume
} else {
  // Step 1부터 시작
}
```

---

## 4. Flutter 파일 구조 제안

```
frontend/lib/
├── features/
│   └── onboarding/
│       ├── data/
│       │   ├── models/
│       │   │   ├── onboarding_state.dart          # 온보딩 상태 모델
│       │   │   ├── pet_profile_draft.dart         # 초안 프로필 모델
│       │   │   └── onboarding_step.dart          # 단계 enum
│       │   └── repositories/
│       │       ├── onboarding_repository.dart     # 로컬 저장소 접근
│       │       └── onboarding_repository_impl.dart
│       │
│       ├── domain/
│       │   └── services/
│       │       ├── onboarding_service.dart        # 비즈니스 로직
│       │       └── device_uid_service.dart        # device_uid 생성/관리
│       │
│       └── presentation/
│           ├── controllers/
│           │   ├── onboarding_controller.dart     # Riverpod StateNotifier
│           │   └── onboarding_state.dart          # 상태 클래스
│           │
│           ├── screens/
│           │   ├── onboarding_wrapper.dart        # 전체 플로우 관리
│           │   ├── step_01_welcome_nickname.dart
│           │   ├── step_02_pet_name.dart
│           │   ├── step_03_species_selection.dart
│           │   ├── step_04_birth_mode.dart
│           │   ├── step_05_breed_selection.dart
│           │   ├── step_06_sex_neutered.dart
│           │   ├── step_07_weight.dart
│           │   ├── step_08_body_condition.dart
│           │   ├── step_09_health_concerns.dart
│           │   ├── step_10_food_allergies.dart
│           │   └── step_11_photo_upload.dart
│           │
│           └── widgets/
│               ├── onboarding_header.dart         # 공통 헤더 (진행률 + 뒤로가기)
│               ├── onboarding_footer.dart          # 공통 푸터 (CTA 버튼)
│               ├── progress_bar.dart              # 진행률 바
│               ├── species_card.dart              # 종 선택 카드
│               ├── birth_mode_card.dart            # 생년월일 모드 카드
│               ├── breed_search_bar.dart           # 품종 검색 바
│               ├── breed_chip.dart                # 품종 칩
│               ├── sex_card.dart                  # 성별 카드
│               ├── neutered_card.dart              # 중성화 카드
│               ├── weight_input_pad.dart           # 몸무게 입력 패드
│               ├── bcs_slider.dart                # BCS 슬라이더
│               ├── bcs_character_animation.dart    # BCS 캐릭터 애니메이션
│               ├── chip_selection_grid.dart       # 칩 선택 그리드
│               ├── photo_upload_section.dart       # 사진 업로드 섹션
│               └── emoji_icon.dart                # 큰 이모지 아이콘
│
├── core/
│   └── storage/
│       ├── secure_storage.dart                     # flutter_secure_storage 래퍼
│       └── storage_keys.dart                       # 저장소 키 상수
│
└── app/
    └── router/
        └── onboarding_routes.dart                  # 온보딩 라우트 정의
```

---

### 주요 파일 설명

#### `onboarding_wrapper.dart`
- 전체 온보딩 플로우를 관리하는 래퍼 화면
- `PageView` 또는 `GoRouter`로 단계별 화면 전환
- 상태 관리 및 진행률 계산

#### `onboarding_controller.dart`
- Riverpod `StateNotifier`로 온보딩 상태 관리
- 각 단계별 데이터 저장/로드
- validation 로직
- 완료 시 서버 upsert

#### `onboarding_repository.dart`
- `flutter_secure_storage`를 통한 로컬 저장소 접근
- `device_uid` 생성/조회
- 초안 프로필 저장/로드

#### `device_uid_service.dart`
- UUID v4 생성
- Secure Storage에 저장
- 기존 `device_uid` 조회

---

## 5. "귀찮지 않게" 만드는 디테일 10가지

### 1️⃣ 진행률 표시 + 동기화된 문구

**구현:**
- 상단에 시각적 진행률 바 (0~100%)
- 각 화면에 "1 / 11" 텍스트 표시
- 진행률에 따라 동기화된 격려 문구
  - 0~30%: "시작이 반이에요! 💪"
  - 30~60%: "잘하고 있어요! ✨"
  - 60~90%: "거의 다 왔어요! 🎉"
  - 90~100%: "마지막 단계예요! 🏁"

**효과:** 사용자가 얼마나 남았는지 명확히 인지, 포기율 감소

---

### 2️⃣ 랜덤 닉네임 추천 + 자동완성

**구현:**
- "🎲 추천받기" 버튼 클릭 시 랜덤 닉네임 생성
- 닉네임 풀: 반려동물 관련 + 귀여운 조합
  - 예: "뽀뽀맘", "멍멍이집사", "냥이사랑", "골든맘", "츄츄파파"
- 입력 중 자동완성 제안 (선택 사항)

**효과:** 결정 부담 감소, 빠른 진행

---

### 3️⃣ 기본값 + 스마트 추론

**구현:**
- **몸무게**: 종/나이 기반 추정값 제시 (예: 소형견 3kg, 중형견 10kg)
- **BCS**: 5 (중간값) 기본 선택
- **중성화**: "잘 모르겠어요" 기본 선택 (부담 감소)
- **건강 고민/알레르기**: "없어요" 기본 선택

**효과:** 빠른 선택 가능, 사용자 부담 감소

---

### 4️⃣ 한 화면 = 한 질문 + 큰 터치 영역

**구현:**
- 각 화면에 하나의 핵심 질문만
- 선택 카드: 최소 48pt 높이, 넓은 패딩
- 터치 영역 확장 (카드 외부 여백도 클릭 가능)

**효과:** 인지 부담 감소, 한 손 조작 용이

---

### 5️⃣ 자동 스크롤 + 키보드 대응

**구현:**
- 입력 필드 포커스 시 자동 스크롤
- `KeyboardAvoidingView`로 키보드에 가려지지 않도록
- 다음 버튼 클릭 시 키보드 자동 닫기

**효과:** 입력 편의성 향상

---

### 6️⃣ 즉시 피드백 + 부드러운 애니메이션

**구현:**
- 입력 즉시 validation (에러 메시지 부드럽게 표시)
- 선택 시 haptic feedback (가볍게)
- 화면 전환: `Hero` 애니메이션 또는 `FadeTransition`
- BCS 슬라이더: 캐릭터 실루엣 부드럽게 변화
- 첫 BCS 선택 시 하트 팝업 애니메이션

**효과:** 반응성 향상, 즐거운 경험

---

### 7️⃣ 검색 + 인기 항목 우선 노출

**구현:**
- **품종 선택**: 검색 바 + 인기 품종 상단 고정
  - 인기 품종: 골든리트리버, 말티즈, 푸들, 비글, 치와와 등
- **건강 고민/알레르기**: 자주 선택되는 항목 상단 배치

**효과:** 빠른 선택, 검색 편의성

---

### 8️⃣ 스킵 가능 + "나중에" 옵션

**구현:**
- **사진**: "건너뛰기" 버튼 명시
- **품종 (고양이)**: "잘 모르겠어요" 옵션
- **중성화**: "잘 모르겠어요" 옵션
- **건강 고민/알레르기**: "없어요" 옵션

**효과:** 부담 감소, 완료율 향상

---

### 9️⃣ 초안 저장 + 재진입 시 resume

**구현:**
- 각 단계 완료 시 자동 저장 (Secure Storage)
- 앱 종료 후 재진입 시 마지막 단계로 복귀
- 입력한 데이터 유지

**효과:** 중단해도 안심, 완료율 향상

---

### 🔟 따뜻한 카피 + 이모지 활용

**구현:**
- 모든 문구에 "우리 아이" 표현 사용
- 이모지로 시각적 즐거움 제공
- 격려 문구: "잘하고 있어요!", "거의 다 왔어요!"
- 에러 메시지도 부드럽게: "이 정보는 꼭 필요해요"

**효과:** 친근함, 긍정적 경험

---

## 추가 구현 팁

### 애니메이션 예시

```dart
// BCS 캐릭터 애니메이션
AnimatedContainer(
  duration: Duration(milliseconds: 300),
  curve: Curves.easeInOut,
  child: CustomPaint(
    painter: CharacterSilhouettePainter(bcs: bcs),
  ),
)

// 하트 팝업 (첫 선택 시)
if (showHeartPop)
  Positioned(
    child: TweenAnimationBuilder(
      tween: Tween<double>(begin: 0, end: 1),
      duration: Duration(milliseconds: 600),
      builder: (context, value, child) {
        return Transform.scale(
          scale: value,
          child: Opacity(
            opacity: 1 - value,
            child: Icon(Icons.favorite, color: Colors.red),
          ),
        );
      },
    ),
  )
```

### Validation 예시

```dart
String? validateNickname(String? value) {
  if (value == null || value.trim().isEmpty) {
    return "닉네임을 입력해주세요";
  }
  if (value.trim().length < 2) {
    return "닉네임은 2자 이상이어야 해요";
  }
  if (value.trim().length > 12) {
    return "닉네임은 12자 이하여야 해요";
  }
  return null;
}
```

### Secure Storage 예시

```dart
class OnboardingRepository {
  final SecureStorage _storage;
  
  Future<String?> getDeviceUid() async {
    String? uid = await _storage.read('device_uid');
    if (uid == null) {
      uid = _generateUuid();
      await _storage.write('device_uid', uid);
    }
    return uid;
  }
  
  Future<void> saveDraftProfile(PetProfileDraft draft) async {
    await _storage.write('draft_pet_profile', jsonEncode(draft.toJson()));
  }
  
  Future<PetProfileDraft?> loadDraftProfile() async {
    String? json = await _storage.read('draft_pet_profile');
    if (json == null) return null;
    return PetProfileDraft.fromJson(jsonDecode(json));
  }
}
```

---

## 완료 체크리스트

- [ ] 11개 화면 모두 구현
- [ ] 진행률 바 및 단계 표시
- [ ] 뒤로가기 버튼 (첫 화면 제외)
- [ ] 각 단계 validation
- [ ] 초안 자동 저장
- [ ] 재진입 시 resume
- [ ] device_uid 생성/저장
- [ ] 서버 upsert (완료 시)
- [ ] 메인 홈으로 replace navigation
- [ ] 애니메이션 (BCS, 화면 전환)
- [ ] haptic feedback
- [ ] 키보드 대응
- [ ] 접근성 (큰 글씨, 최소 터치 영역)

---

**Made with ❤️ for HeyZeno**
